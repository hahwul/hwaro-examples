+++
title = "API"
description = "Manifold API reference for admin and tenant operations"
tags = ["api", "reference", "manifold"]
+++

This section covers the Manifold API surface. The platform exposes two separate API servers:

- **Admin API** (port 3001) -- Manage tenants, tiers, and platform configuration
- **Tenant API** (port 3000) -- Tenant-scoped operations for application data

Start with [Admin API](admin-api/) for platform management, review [Tenant API](tenant-api/) for application-level endpoints, and see [Authentication](authentication/) for token and session management.
