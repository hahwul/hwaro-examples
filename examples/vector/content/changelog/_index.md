+++
title = "Changelog"
description = "Every tagged release of vector, newest first, with the migrations and CLI changes that shipped in it."
sort_by = "date"
template = "section"
generate_feeds = true
+++

Every tagged release, in order. Patch releases only fix bugs; minor releases
may add commands or change a default, but never break a `.vx` migration file
that already applied cleanly.
