+++
title = "The restoration log"
description = "Every machine that has crossed the Foehn bench, newest first"
sort_by = "date"
reverse = true
paginate = 5
template = "section"
generate_feeds = true
+++

Every machine that has crossed the bench, newest first. Each entry is one
job on one instrument — a power supply, a keybed, a bank of oscillators —
written up the week the solder cooled.
