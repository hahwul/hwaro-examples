+++
title = "Flint"
description = "Deterministic 2D physics for game engines, one fixed timestep at a time"
template = "home"
+++

flint is a small, dependency-free 2D rigid-body module for game engines. It runs on a single fixed timestep with integer-anchored math, so the same inputs produce the same bytes on every machine — the foundation for lockstep multiplayer and frame-perfect rollback.
