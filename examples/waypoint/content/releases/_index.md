+++
title = "Releases"
description = "Every tagged Waypoint build, newest first — rootless by default, lazy image pulls, and a deliberately small trusted computing base."
sort_by = "date"
template = "section"
generate_feeds = true
+++

Every tagged build of Waypoint, newest first. Patch releases carry only fixes;
minor releases may change defaults but never break a pinned digest.
