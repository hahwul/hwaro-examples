+++
title = "The Pattern Wall"
description = "Six live interference fields, each labelled with its line pitch and the rotation of its overlapping layers."
sort_by = "date"
reverse = false
template = "section"
generate_feeds = false
+++

Every tile below is a real moiré: two or three line fields laid over one another until the eye reads a third, larger pattern that exists in neither layer alone. Tap any field to read its recipe — the pitch, the angles, and the beat frequency it produces. New here? Start with the [reading guide](/about/).
