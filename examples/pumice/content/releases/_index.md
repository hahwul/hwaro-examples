+++
title = "Releases"
description = "Version history for the Pumice agent."
sort_by = "date"
template = "section"
generate_feeds = true
+++

Every release is a signed static binary. Version numbers follow semver; probe
ABI changes are called out explicitly because they may require a kernel with a
newer BTF than the previous line supported.
