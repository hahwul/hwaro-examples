+++
title = "Search"
description = "Search the tide log by species, cove, or tag."
template = "search"
in_search_index = false
+++

Search across every logged visit — titles, cove names, and the finds written into each entry. Try a species like `urchin` or `chiton`, a cove like `Roig`, or a tag like `anemones`.
