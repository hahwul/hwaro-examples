+++
title = "Releases"
description = "Every Ohm release, newest first — infrastructure changes, new endpoints, and retention updates, all computed and shipped from EU soil."
sort_by = "date"
reverse = true
template = "section"
generate_feeds = true
+++

The full changelog for Ohm's collection pipeline, dashboard, and API.
Nothing here is marketing copy — each entry lists exactly what moved, what
it means for existing projects, and whether any action is required.
