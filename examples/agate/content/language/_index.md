+++
title = "Language Reference"
description = "Seven chapters, read in order: delimiters, whitespace, values, control flow, filters, composition, and the compiler that turns all of it into a function."
sort_by = "weight"
template = "section"
+++

Each chapter builds on the last. If you are new to Agate, start with delimiters and read straight through — the whitespace rules in Chapter&nbsp;2 are the load-bearing wall the rest of the language stands on.
