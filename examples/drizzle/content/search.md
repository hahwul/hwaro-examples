+++
title = "Search the notebook"
template = "search"
description = "Search every sourdough experiment on Drizzle by variable, flour, or failure mode"
in_search_index = false
+++

Type a word and the whole notebook is searched — every experiment, every
schedule, every honest failure. Try a variable (*hydration*, *rye*), a
technique (*autolyse*, *retard*), or an outcome (*gummy*, *oven spring*).
