+++
title = "Issues"
description = "Every issue Missive has filed from the Fenwick Test Floor, newest specimen first."
sort_by = "date"
reverse = true
paginate = 6
template = "section"
generate_feeds = true
+++

The full specimen archive. Each issue opens with the batch record — material, lot number, and the chip that stands in for its raw color — before the letter gets into what the test actually did to it.
