+++
title = "Logbook"
description = "Every worked station and antenna note, newest first — UTC, frequency, and mode logged at the key."
sort_by = "date"
reverse = true
paginate = 6
template = "section"
generate_feeds = true
+++

The book itself: contact stories, antenna builds, and QRP field days in one
stream, newest at the top. Each row carries the UTC timestamp, the frequency in
megahertz, and the mode I ran. Follow a callsign into the write-up, or grep the
whole log with `Ctrl`/`Cmd` + `K`.
