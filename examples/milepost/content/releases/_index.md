+++
title = "The strip"
description = "Every Milepost server release, newest first — each one a marker on the road. Semver on the plate, the shift written plainly below it."
sort_by = "date"
template = "section"
generate_feeds = true
+++

Milepost follows semantic versioning. Major markers move the plugin API contract, minor markers add capabilities without breaking your plugins, and patch markers fix the road surface without changing the map.
