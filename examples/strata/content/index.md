+++
title = "Release ledger"
description = "Release history for a multi-language static analysis engine with incremental caching"
template = "home"
+++

Strata analyzes eleven languages from a single dependency graph and re-checks
only what changed between runs. This ledger tracks every rule the engine has
added, fixed, and removed since 3.3.0 — the figures below are lifted straight
from the tool's own report output.
