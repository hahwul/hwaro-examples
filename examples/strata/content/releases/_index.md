+++
title = "Release history"
description = "Seven releases, newest first — rule deltas, cache changes, and upgrade notes."
sort_by = "date"
reverse = true
paginate = 0
template = "section"
generate_feeds = true
+++

Newest first. Each entry opens with the same segment meter you see on the
dashboard — proportional bars for the rules added, fixed, and removed in that
build — followed by the full notes: new rules, notable fixes, anything removed
or deprecated, and how the incremental cache is affected.
