+++
title = "Search"
template = "search"
description = "Search every release note and the architecture reference — rule names, cache internals, language packs."
in_search_index = false
+++

Type a term and the whole history is combed at once: seven releases of notes and
the architecture reference. Try a rule area, a language, or a cache internal.
