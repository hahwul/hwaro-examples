+++
title = "Episodes"
description = "Every Reverb episode, one cipher block at a time — from the Caesar shift to lattice-based schemes built to survive a quantum adversary."
sort_by = "date"
reverse = true
template = "section"
generate_feeds = true
transparent = false
+++

Six blocks so far, each one a cipher pulled apart on the bench: how it was built, how it held, and — eventually — how it fell. Show notes render ciphertext by default. Hover or focus a card to decrypt the preview.
