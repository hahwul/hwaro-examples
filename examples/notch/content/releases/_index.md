+++
title = "Releases"
description = "Every tagged build of Notch, newest first — FM engine changes, pattern editor work, and export fixes in tracker-tight detail."
sort_by = "date"
reverse = false
template = "section"
generate_feeds = true
+++

The complete version history for Notch, the four-operator FM tracker. Each
entry lists what moved in that build: the synthesis engine, the pattern editor,
the sample loader, and the exporters. Rows are numbered in hex the same way the
pattern grid numbers its lines, because that is how we read our own changelog.

Filter the log with the field below — it searches every release note in place.
<!-- more -->
