+++
title = "Manual"
description = "The gabbro manual, section 3 — library functions, from first open() to on-disk format."
sort_by = "weight"
template = "section"
+++

Five chapters, read in order. Each one is written to stand alone as a man page,
but the sequence walks from a first `gb_open()` through the multi-version core and
down to how bytes land on disk. Every function shown here is real to this manual
and stable across the 0.9 line.
